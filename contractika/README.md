# ZEN IT Services - tools synchronisation

```
    /*
        SD API endpoints:

        GET    AbsencesWebService.asmx
        GET    AbsencesWebService.asmx/GetEmployees"
    */

    /*
        AT API endpoints :

        GET    Tickets/query?search={...}
        GET    Tickets/:id
        GET    Countries/:id
        GET    ClassificationIcons/:id
        POST   Contracts/:id/Blocks
        POST   Appointments/:id
        PATCH  Appointments/:id
        DELETE Appointments/:id
        GET    HolidaySets/:id/Holidays
        POST   Opportunities
        GET    Resources/:id
        GET    Resources/query?search={...}
    */
```